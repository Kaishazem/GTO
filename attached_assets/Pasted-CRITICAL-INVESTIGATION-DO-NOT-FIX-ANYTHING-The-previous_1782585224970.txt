CRITICAL INVESTIGATION — DO NOT FIX ANYTHING

The previous investigation identified several possible root causes, but we still do not have the missing evidence required to determine the real one.

Your task is to expose the complete data flow WITHOUT changing application behaviour.

DO NOT:

- modify business logic
- modify import logic
- modify postback logic
- modify Start Task logic
- modify reward calculation
- modify routing
- modify Firestore schema
- modify URL generation

ONLY expose and display the missing information.

---

PART 1 — Preserve the COMPLETE Raw Platform Response

Currently the importer discards the original platform payload (_raw).

Change the importer so that every imported task stores the FULL original platform response in Firestore.

Example:

rawPlatformResponse: { ...entire original offer object... }

Nothing should be removed.

---

PART 2 — Admin Task Details

When an admin opens Task Details, display ALL of the following fields exactly as stored:

Firestore Document ID

externalId

title

description

url

trackingUrl

previewUrl

image

requirements

countries

devices

conversionType

platform

platformId

reward

payout

status

networkStatus

active

taskType

createdAt

AND

Display the entire rawPlatformResponse object in a collapsible JSON viewer.

No truncation.

---

PART 3 — URL Investigation

Inside Task Details create a section called:

"URL TRACE"

Display:

Original Platform URL

Tracking URL

Preview URL

Current URL stored in Firestore

Final URL generated by Start Task

Final URL after adding s1

Final URL after adding s2

Show every URL exactly.

Do not hide query parameters.

---

PART 4 — Start Task Trace

Without changing behaviour, log the complete execution:

User clicks Start Task

↓

URL before modification

↓

URL after s1 appended

↓

URL after s2 appended

↓

Final opened URL

This should be visible in browser console.

---

PART 5 — ID Investigation

Display both:

Firestore Document ID

externalId

and explicitly indicate which one is currently used for:

- Start Task
- s2 parameter
- Postback matching

---

PART 6 — Import Mapping

Inside Admin create a debug section called:

IMPORT MAPPING

For one selected task show:

RAW PLATFORM FIELD

↓

NORMALIZED FIELD

↓

FIRESTORE FIELD

↓

FRONTEND TASK OBJECT

↓

DISPLAYED VALUE

This should reveal where any field disappears.

---

PART 7 — Final Report

Do NOT fix anything.

Report only:

1. Is trackingUrl different from url?

2. Which URL is currently opened by Start Task?

3. Which URL came from the platform?

4. Is externalId different from Firestore Document ID?

5. Which one is used in s2?

6. Is any field lost between Raw Response and Firestore?

7. Is any field lost between Firestore and TaskContext?

8. Screenshot (or description) of the new Task Details debug panel.

Do not perform any fixes until this investigation is complete.