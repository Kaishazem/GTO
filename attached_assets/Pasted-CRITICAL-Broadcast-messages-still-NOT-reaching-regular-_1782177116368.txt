CRITICAL: Broadcast messages still NOT reaching regular users

Current Status:
- ✅ Admin can send broadcast (no 500 error)
- ✅ Message saved to archive
- ✅ Message appears in Admin's notifications
- ❌ Regular users (e.g., hazem1) do NOT see the broadcast
-  All other fixes were applied but delivery still broken

DEBUGGING REQUIRED:

Step 1: Check Firestore Collections
- Open Firestore Console
- Check what collections exist: "broadcasts", "notifications", "messages"?
- When Admin sends broadcast, which collection gets the document?
- What fields are in that document?
- Is there a "userId" field? What is its value?

Step 2: Check Broadcast Route Logic
In server/routes/broadcast.ts:
- Does it create ONE document for all users?
- OR does it create SEPARATE document for each user?
- If ONE document: this is the problem - users can't see it
- If SEPARATE documents: check if userId matches user's actual UID

Step 3: Check User Notification Query
In NotificationContext.tsx or wherever user reads notifications:
- What collection is it querying?
- What is the filter? (userId == ?)
- Does the query match where broadcast saves documents?

Step 4: Add Debug Logging
Add console.log in broadcast route:
- Log: "Creating notification for user: [userId]"
- Log: "Notification saved to collection: [collectionName]"
- Log: "Total users found: [count]"

Add console.log in user notification query:
- Log: "User [uid] listening to collection: [collectionName]"
- Log: "Query filter: userId == [uid]"
- Log: "Notifications found: [count]"

Step 5: Fix the Mismatch
Most likely issue:
- Broadcast saves to "broadcasts" collection
- User listens to "notifications" collection
- OR broadcast doesn't include userId field
- OR broadcast creates one doc instead of per-user docs

REQUIRED FIX:
Modify broadcast route to:
1. Get all users from "users" collection
2. For EACH user, create a document in "notifications" collection:
   {
     userId: user.uid,
     title: broadcast.title,
     message: broadcast.message,
     type: "broadcast",
     read: false,
     createdAt: serverTimestamp()
   }
3. Verify documents are created

TEST:
1. Send broadcast from Admin
2. Check Firestore - verify documents created for each user
3. Open user account - verify notification appears
4. Show me the Firestore screenshot and logs

DO NOT modify other features.
Only fix broadcast delivery.