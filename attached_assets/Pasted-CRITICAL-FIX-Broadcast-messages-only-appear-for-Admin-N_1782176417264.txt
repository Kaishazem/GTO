CRITICAL FIX: Broadcast messages only appear for Admin, NOT for regular users

Current Status:
- ✅ Broadcast send works (saved to archive)
- ✅ Messages appear in Admin's notification panel
-  Messages do NOT appear for regular users (e.g., hazem1)
- ❌ "Mark all read" button does not work without page refresh
- ❌ Delete notification does not work without page refresh
- ❌ New notifications do not appear in real-time (requires page refresh)

REQUIRED FIXES:

1. BROADCAST DELIVERY TO ALL USERS
Problem: Messages only show for Admin who sent them
Fix:
- When Admin sends broadcast, create notification document for EACH user
- Get all users from Firestore "users" collection
- For each user, create a notification in "notifications" collection with:
  * userId: user's UID
  * title: broadcast title
  * message: broadcast message
  * timestamp: server timestamp
  * read: false
  * type: "broadcast"

2. REAL-TIME NOTIFICATIONS
Problem: Notifications only appear after page refresh
Fix:
- In NotificationContext.tsx, use onSnapshot() instead of getDocs()
- Subscribe to notifications collection filtered by current user's UID
- Listen for real-time updates

3. MARK ALL READ
Problem: Button does not work without refresh
Fix:
- Update "read" field to true in Firestore for all user's notifications
- Update local state immediately (don't wait for refresh)
- Use batch write for performance

4. DELETE NOTIFICATION
Problem: Delete only works after refresh
Fix:
- Delete document from Firestore
- Remove from local state immediately
- Update UI without refresh

FILES TO CHECK:
- server/routes/broadcast.ts (delivery logic)
- src/contexts/NotificationContext.tsx (real-time subscription)
- src/components/NotificationBell.tsx (UI updates)
- src/pages/AdminPage.tsx (broadcast send)

TESTING:
1. Send broadcast from Admin
2. Open user account (hazem1) in different browser/incognito
3. Verify notification appears WITHOUT refresh
4. Click "Mark all read" - verify it works immediately
5. Delete notification - verify it disappears immediately

DO NOT modify other features.
Only fix broadcast delivery and notification UI.